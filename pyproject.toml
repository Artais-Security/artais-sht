[project]
name = "sht"
version = "0.1.0"
description = "HTTP Security Headers Tester"
readme = "README.md"
requires-python = ">=3.10"
dependencies = ["httpx>=0.25.0"]

[project.scripts]
sht = "sht.cli:main"

[tool.pytest.ini_options]
pythonpath = ["src"]
